# Pr# Tailscale Kubernetes Operator Configuration
# OAuth Client: Create at https://login.tailscale.com/admin/settings/oauth
# Required scopes: device:read,device:write
TAILSCALE_OAUTH_CLIENT_ID=k123456789012345678
TAILSCALE_OAUTH_CLIENT_SECRET=tskey-client-secret-YOUR-SECRET-HERE

# Tailscale Configuration
# Individual Auth Keys for Each Service (legacy approach for sidecars)
# Get auth keys from: https://login.tailscale.com/admin/settings/keys
# Recommended settings: Reusable=Yes, Ephemeral=No, Tags=tag:k8s
# Create separate keys for each service to avoid conflicts

# ArgoCD and general services
TAILSCALE_AUTH_KEY=tskey-auth-YOUR-KEY-HERE
TAILSCALE_SIDECAR_AUTH_KEY=tskey-auth-YOUR-SIDECAR-KEY-HERE
TAILSCALE_SUBNET_AUTH_KEY=tskey-auth-YOUR-SUBNET-KEY-HERE

# Observability services (individual keys prevent conflicts)
TAILSCALE_PROMETHEUS_AUTH_KEY=tskey-auth-YOUR-PROMETHEUS-KEY-HERE
TAILSCALE_GRAFANA_AUTH_KEY=tskey-auth-YOUR-GRAFANA-KEY-HERE
TAILSCALE_LOKI_AUTH_KEY=tskey-auth-YOUR-LOKI-KEY-HERE

# Tailscale API Configuration (optional - for automation)
# API Key: Get from https://login.tailscale.com/admin/settings/keys (create an API key, not auth key)
# Tailnet: Your tailnet name (email@domain.com or organization name)
TAILSCALE_API_KEY=tskey-api-YOUR-API-KEY-HERE
TAILSCALE_TAILNET=your-email@domain.comvironment Variables
# Copy this file to .env and fill in your actual values
# Note: .env is gitignored for security

# Tailscale Configuration
# Get your auth key from: https://login.tailscale.com/admin/settings/keys
# Recommended settings: Reusable=Yes, Ephemeral=No, Tags=tag:k8s
# Tailscale Auth Key for Kustomize secretGenerator
# Get from: https://login.tailscale.com/admin/settings/keys
# For subnet router: Enable "Advertise routes" option
# Recommended: Enable "Reusable" for testing
TAILSCALE_AUTH_KEY=tskey-auth-YOUR-KEY-HERE

# Terraform Variables (if needed)
# These can supplement terraform.tfvars for sensitive values
# TF_VAR_project_id=your-gcp-project-id
# TF_VAR_region=us-central1